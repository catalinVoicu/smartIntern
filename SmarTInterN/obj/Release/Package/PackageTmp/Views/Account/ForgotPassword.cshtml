@model SmarTInterN.Models.Account.ViewModels.ForgotPasswordModel

@{
    ViewBag.Title = "ForgotPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ForgotPassword</h2>

<div class="registrationForm form">
    @using (Html.BeginForm("ForgotPassword", "Account"))
    {
         <div class="row">
            @Html.LabelFor(p => p.UserName, "Your User Name:")
            @Html.TextBoxFor(p => p.UserName)

            <span class="tooltip">
                @Html.ValidationMessageFor(p => p.Email)
            </span>
        </div>
        @*<div class="row">
            @Html.LabelFor(p => p.Email, "Your email:")
            @Html.TextBoxFor(p => p.Email)

            <span class="tooltip">
                @Html.ValidationMessageFor(p => p.Email)
            </span>
        </div>*@
        <div class="row buttonRow">
            <button type="submit" class="button">
                <span>Send</span>
            </button>
            <div class="cancelButton right">@Html.ActionLink("Cancel", "Index", "Home")</div>
        </div>
    }
</div>